# Client env (names only)

# Base URL of the Express API, including /api/v1 if that is how the client calls routes.
NEXT_PUBLIC_API_URL=

# JWT secret used by src/proxy.ts. This is not the server env name ACCESS_TOKEN_SECRET.
JWT_ACCESS_SECRET=
